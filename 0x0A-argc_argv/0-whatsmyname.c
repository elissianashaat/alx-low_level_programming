#include <stdio.h>

/**
 * main - entry point
 * @argc: number of arguments
 * @argv: value passed to arguments
 * Return: 0
 */

int main(int argc, char const *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
